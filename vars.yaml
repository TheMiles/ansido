newuser: miles
user_pwd: "{{ user_pwd_vault }}"

docker_base_dir: /var/docker
dkim_dir: "/etc/dkim/keys/"

host_info: {
  scabb: {
    hostname: 'lauschwaren.de',
    mail: true
  },
  phatt: {
    hostname: 'bannbaladin.de',
    mail: false
  }
  }