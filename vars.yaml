newuser: miles
ssh_key: https://github.com/TheMiles.keys
user_pwd: "{{ user_pwd_vault }}"

docker_base_dir: /var/docker
mailserver_base_dir: "{{ docker_base_dir }}/mailserver"
dkim_dir: "/etc/dkim/keys/"